package com.orientesanasrekinatajs.domain.model

/**
 * The unit system used to display distances in the UI.
 *
 * [METRIC] shows meters/kilometers; [IMPERIAL] shows feet/miles.
 */
enum class DistanceUnit {
    METRIC,
    IMPERIAL,
}
