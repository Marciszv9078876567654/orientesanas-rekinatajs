package com.orientesanasrekinatajs.domain.model

/**
 * The user's theme preference.
 *
 * [SYSTEM] follows the device setting, [LIGHT] forces light mode, and [DARK] forces dark mode.
 */
enum class ThemeConfig {
    SYSTEM,
    LIGHT,
    DARK,
}
